# AI Assistant (Antigravity) Context & Rules

This file (`GEMINI.md`) contains custom instructions and context for the AI Assistant. 
The AI will read this file automatically upon starting a new session.

## Project Context
- **Project Name**: P2P Virtual Mesh Network
- **Tech Stack**: Go, libp2p, Wails (Go + Web frontend), Vite (Vanilla JS).
- **Architecture**: A desktop application (GUI) and CLI node built for cross-region, NAT-traversal peer-to-peer networking.

## General Rules
1. **Code Style**: When writing Go code, adhere to standard Go formatting (`gofmt`).
2. **Frontend UI**: Maintain a premium, glassmorphism aesthetic for the Wails frontend. Use Vanilla JS and CSS for UI updates unless a heavy framework is explicitly requested.
3. **Documentation**: Refer to `README.md` for project installation, deployment, and usage instructions.

*Add any custom rules or prompt modifiers you want the AI to follow here.*
